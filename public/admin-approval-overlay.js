(function(){
  if(localStorage.getItem('carplay_admin_here')!=='1')return;
  const secret=localStorage.getItem('carplay_admin_secret')||'';if(!secret)return;
  const style=document.createElement('style');style.textContent='#adminApprovalOverlay{position:fixed;inset:0;z-index:999999;background:rgba(0,0,0,.84);display:none;align-items:center;justify-content:center;padding:18px}#adminApprovalCard{width:min(520px,100%);max-height:94vh;overflow:auto;background:#0c1119;color:#fff;border:4px solid #f39b19;border-radius:24px;padding:22px;text-align:center;font:900 18px Arial}#adminApprovalCard h2{color:#f39b19}#adminApprovalActions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:18px}#adminApprovalActions button{min-height:62px;border:0;border-radius:14px;color:#fff;font-size:20px;font-weight:900}#adminApprovalReview{grid-column:1/-1;background:#087ee5}#adminApprovalYes{background:#158347}#adminApprovalNo{background:#b52f38}';document.head.appendChild(style);
  const box=document.createElement('div');box.id='adminApprovalOverlay';box.innerHTML='<div id="adminApprovalCard"><h2>🔔 DEMANDE DE MODIFICATION</h2><div id="adminApprovalText"></div><div id="adminApprovalActions"><button id="adminApprovalReview">👁️ CONSULTER LA FICHE</button><button id="adminApprovalYes">OUI</button><button id="adminApprovalNo">NON</button></div></div>';document.body.appendChild(box);
  let current=null,busy=false;const safe=v=>String(v||'').replace(/[<>&]/g,'');
  function review(){
    if(!current)return;
    const marketKey=String(current.market_key||''),marketName=String(current.market_name||'Marché'),back=location.pathname+location.search+location.hash;
    try{localStorage.setItem('verify_form_payload',JSON.stringify({key:marketKey,name:marketName,values:{}}));localStorage.setItem('carplay_admin_review_request',String(current.id||''))}catch(e){}
    location.href='/verification-v9.html?mk='+encodeURIComponent(marketKey)+'&n='+encodeURIComponent(marketName)+'&adminReview=1&back='+encodeURIComponent(back);
  }
  async function decide(approve){if(!current||busy)return;busy=true;try{await fetch('/api/admin/gps-unlock-requests',{method:'POST',headers:{'content-type':'application/json',authorization:'Bearer '+secret},body:JSON.stringify({id:current.id,approve:approve})});box.style.display='none';current=null}finally{busy=false;setTimeout(load,300)}}
  document.getElementById('adminApprovalReview').onclick=review;document.getElementById('adminApprovalYes').onclick=()=>decide(true);document.getElementById('adminApprovalNo').onclick=()=>decide(false);
  async function load(){if(current||busy)return;try{const r=await fetch('/api/admin/gps-unlock-requests',{headers:{authorization:'Bearer '+secret},cache:'no-store'}),j=await r.json(),x=j.requests&&j.requests[0];if(!r.ok||!x)return;current=x;const actions={time:"modifier l’horaire",count:"modifier le nombre de commerçants",draw:"modifier le tirage au sort",welcome:"modifier l’accueil du placier",placer:"modifier le responsable",clientModel:"modifier le modèle de clients",photo:"remplacer la photo",gps:"modifier le point GPS"},action=actions[x.scope]||"modifier la fiche";document.getElementById('adminApprovalText').textContent=safe(x.requester_name||'Une personne')+(x.requester_email?' — '+safe(x.requester_email):'')+' veut '+action+' du marché '+safe(x.market_name||'Marché')+(x.current_value?' — valeur actuelle : '+safe(x.current_value):'')+(x.proposed_value?' — nouvelle valeur : '+safe(x.proposed_value):'')+'. Consultez la fiche si nécessaire, puis choisissez OUI ou NON.';box.style.display='flex'}catch(e){}}
  load();setInterval(load,3000);
})();
